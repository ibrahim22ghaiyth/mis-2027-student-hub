# حماية MIS 2027

هذه النسخة مهيأة بحماية أقوى على مستوى الخادم ولوحة الإدارة والملفات.

## الحماية المفعلة

- كلمات مرور الإدارة مخزنة باستخدام PBKDF2-SHA256 مع 600,000 دورة وملح عشوائي مستقل.
- كلمة المرور الجديدة يجب أن تكون 14 حرفاً على الأقل وبتركيب قوي.
- إيقاف مؤقت لمحاولات تسجيل الدخول المتكررة، مع Rate Limiting إضافي.
- جلسة الإدارة HttpOnly وSameSite=Strict، وتصبح Secure تلقائياً عند HTTPS.
- الجلسة محدودة زمنياً، وتنتهي عند الخمول الطويل، ومربوطة ببصمة User-Agent.
- حماية CSRF لجميع عمليات الإدارة التي تغيّر البيانات.
- التحقق من Origin للطلبات التي تغيّر البيانات.
- ترويسات حماية للمتصفح تشمل CSP وnosniff وReferrer-Policy وPermissions-Policy وHSTS عند HTTPS.
- إخفاء تفاصيل إصدار الخادم من HTTP responses.
- ملفات الشروحات HTML تعرض كما رُفعت، لكن داخل Sandbox معزول عن جلسة الإدارة.
- حظر صيغ الملفات التنفيذية والخطرة، وحظر الملفات التي تحمل توقيعات تنفيذية حتى لو تم تغيير امتدادها.
- الملفات المرفوعة لا توضع داخل مجلد الويب العام، وأسماؤها الداخلية عشوائية.
- سجل تدقيق Audit Log داخلي لعمليات الإدارة المهمة.
- نسخ احتياطية تلقائية لقاعدة البيانات عند بدء النظام، مع الاحتفاظ بآخر 5 نسخ.
- أذونات ملفات مقيدة قدر الإمكان على الأنظمة التي تدعم chmod.
- حد أقصى 30 MB للملف وحدود لحجم الطلبات ومدخلات النصوص.
- بيانات الأخطاء الحساسة لا تُرسل للمتصفح.

## عند النشر على الإنترنت

الحماية داخل الكود ليست بديلاً عن إعداد الخادم. استخدم HTTPS حصراً، ولا تنشر ملف `data/initial_admin_credentials.txt`، وخزّن مجلدي `data` و`uploads` في تخزين دائم مع نسخ احتياطية.

لا توجد حماية برمجية تجعل الكود غير قابل للتعديل بواسطة شخص يملك ملفات المشروع وصلاحية الوصول إليها. الحماية هنا تستهدف الاستخدام الفعلي والهجمات عبر الويب، وليست ادعاءً بمنع مالك الملفات من تعديل المصدر.
