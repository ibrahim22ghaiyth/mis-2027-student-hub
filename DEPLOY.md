# النشر المجاني على الإنترنت (بدون تكلفة)

هذه المنصة (Python + SQLite + ملفات مرفوعة) تحتاج خادم Python يعمل دائماً. تحت أرخص الطرق **مجانية بالكامل**.

## قبل الرفع (مهم)

1. **بيانات الإدارة** (`data/initial_admin_credentials.txt`): لا ترفعها للواجهة العامة — غيّر كلمة المرور بعد أول دخول.
2. `.gitignore` يمنع رفع `data/*` و`uploads/*` — المحتوى الحالي يبقى محلياً. بعد النشر ارفع الملفات من لوحة الإدارة.
3. `FORCE_HTTPS=1` مفعّل في `render.yaml` لأن الطلاب يفتحون عبر HTTPS.

---

## الخيار الأسهل: Render (مجاني)

1. أنشئ حساب مجاني على [github.com](https://github.com) وارفع مجلد المشروع:
   ```bash
   git init
   git add .
   git commit -m "MIS 2027"
   git remote add origin https://github.com/YOUR_NAME/mis-2027.git
   git push -u origin main
   ```
2. سجّل في [render.com](https://render.com) → **New +** → **Blueprint** → اختر الريبو.
3. Render يقرأ `render.yaml` + `Dockerfile` تلقائياً ويعطيك رابط مثل:
   `https://mis-2027-xxxx.onrender.com`
4. افتح الرابط → `/admin` → سجّل دخول وأضف المواد والملفات.

**ملاحظات Render المجاني:**
- قد يسبح (sleep) بعد فترة خمول — أول زيارة بعدها تاخذ ~30 ثانية ثم يرجع سريع.
- القرص **مؤقت**: بعد كل إعادة نشر/سبات قد تضيع الرفعات — احتفظ بنسخة من الملفات على جهازك، أو فعّل disk مدفوع لاحقاً إذا احتجت.

---

## بديل: أي استضافة Python مجانية تدعم Docker

- **Fly.io** / **Railway** / **Koyeb**: أنشئ تطبيق Docker واجعل البيئة:
  - `HOST=0.0.0.0`
  - `PORT` (غالباً يحدده المنصة)
  - `FORCE_HTTPS=1`
- الخادم يقرأ `PORT` و`HOST` من البيئة تلقائياً (`server.py`).

---

## تشغيل محلي (نفس ما تستخدمه الآن)

```bash
# ويندوز
ابدأ-MIS-2027.bat

# لينكس/ماك
HOST=127.0.0.1 PORT=8127 python3 server.py
```

رابط محلي: `http://127.0.0.1:8127`

---

## الأداء لعدد كبير من الزوار

- الخادم `ThreadingHTTPServer` + طابور 128 اتصال.
- الملفات المثبّتة (`app.js?v=...`, `styles.css?v=...`) تُرسل `Cache-Control: public, max-age=31536000, immutable` — المتصفح ما يعيد تحميلها إلا عند تغيير `?v=`.
- `index.html` و `service-worker.js` يبقى `no-store` لتحدث فوري.
- للأوفلاين: زر **حفظ للأوفلاين** في الشريط العلوي **وفي القائمة الجانبية (الموبايل)**.

---

## بعد النشر — قائمة تحقق

- [ ] الرابط العام يفتح بالهاتف والكمبيوتر
- [ ] `/admin` يفتح وكلمة المرور تشتغل
- [ ] رفع مادة + ملف محاضرة
- [ ] زر حفظ للأوفلاين من الموبايل
- [ ] Hard Refresh بعد أي تعديل واجهة
